@model List<Portfolio.Models.Proje>
@{
    ViewData["Title"] = "Projeler Yönetimi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <h1><i class="fas fa-folder-open"></i> @ViewData["Title"]</h1>
    <a href="@Url.Action("Create")" class="btn btn-primary">
        <i class="fas fa-plus"></i> Yeni Proje Ekle
    </a>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        <i class="fas fa-check-circle"></i> @TempData["Success"]
    </div>
}

<div class="content-card">
    @if (Model != null && Model.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th width="80">Sıra</th>
                    <th>Başlık</th>
                    <th>Kategori</th>
                    <th>Tarih</th>
                    <th width="100">Durum</th>
                    <th width="200">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.Sira</strong></td>
                        <td>@item.Baslik</td>
                        <td><span class="badge badge-warning">@item.Kategori</span></td>
                        <td>@item.Tarih?.ToString("dd MMM yyyy")</td>
                        <td>
                            @if (item.Aktif)
                            {
                                <span class="badge badge-success">Aktif</span>
                            }
                            else
                            {
                                <span class="badge badge-danger">Pasif</span>
                            }
                        </td>
                        <td>
                            <a href="@Url.Action("Edit", new { id = item.Id })" class="btn btn-warning btn-sm">
                                <i class="fas fa-edit"></i> Düzenle
                            </a>
                            <button onclick="deleteItem(@item.Id)" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash"></i> Sil
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <div style="text-align: center; padding: 50px; color: #999;">
            <i class="fas fa-inbox" style="font-size: 48px; margin-bottom: 20px;"></i>
            <p>Henüz proje eklenmemiş.</p>
        </div>
    }
</div>

@section Scripts {
    <script>
        function deleteItem(id) {
            if (confirm('Bu projeyi silmek istediğinize emin misiniz?')) {
                fetch('@Url.Action("Delete")/' + id, {
                    method: 'POST'
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        location.reload();
                    } else {
                        alert('Silme işlemi başarısız!');
                    }
                });
            }
        }
    </script>
}
